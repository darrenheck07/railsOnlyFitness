<%= form_with(model: latstrapshome4, local: true) do |form| %>
  <% if latstrapshome4.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(latstrapshome4.errors.count, "error") %> prohibited this latstrapshome4 from being saved:</h2>

      <ul>
        <% latstrapshome4.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :exercise %>
    <%= form.text_field :exercise %>
  </div>

  <div class="field">
    <%= form.label :reps %>
    <%= form.text_field :reps %>
  </div>

  <div class="field">
    <%= form.label :sets %>
    <%= form.number_field :sets %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>


<!--
<br> <br><br> <br>
<div class= "container">

  <div class="card">
    <h5 class="card-header">Edit Workout</h5>
    <div class="card-body">
      
          <div class="field form-group">
            <%= form.select(:exercise, options_for_select(['Select Exercise', 'Single Arm Weighted Row', 'Floor Pull Over', 'Weighted/Banded Shrug', 'Test']), {}, {class: "form-control"}) %>
          </div>

          <div class="field form-group">
            <%= form.select(:reps, options_for_select(['Reps', '20', '15', '10']), {}, {class: "form-control"}) %>
          </div>

          <div class="field form-group">
            <%= form.number_field :sets, class:"form-control", placeholder:"Sets" %>
          </div>

          <div class="actions">
            <%= form.submit "Update Exercise", class:"btn btn-dark" %>
          </div>
        <% end %>

    </div>
  </div>
  
</div>-->

