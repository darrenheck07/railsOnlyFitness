<%= form_with(model: chesttricepshome2, local: true) do |form| %>
  <% if chesttricepshome2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chesttricepshome2.errors.count, "error") %> prohibited this chesttricepshome2 from being saved:</h2>

      <ul>
        <% chesttricepshome2.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  
  <br> <br><br> <br>
<div class= "container">

  <div class="card">
    <h5 class="card-header">Edit Workout</h5>
    <div class="card-body">
      
          <div class="field form-group">
            <%= form.select(:exercise, options_for_select(['Select Exercise', 'Weighted Push Ups', 'Decline Bench Press', 'Dumbbell Chest Fly', 'Overhead Dumbbell Extension', 'Dumbbell Kickback' ,'Barbell Skull Crusher']), {}, {class: "form-control"}) %>
          </div>

          <div class="field form-group">
            <%= form.select(:reps, options_for_select(['Reps', '30', '20', '15', '10']), {}, {class: "form-control"}) %>
          </div>

          <div class="field form-group">
            <%= form.number_field :sets, class:"form-control", placeholder:"Sets" %>
          </div>

          <div class="actions">
            <%= form.submit "Update Exercise", class:"btn btn-dark" %>
          </div>
        <% end %>

    </div>
  </div>
  
</div>